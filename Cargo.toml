[workspace]
resolver = "2"
members = ["core", "py-ext"]

[profile.release]
opt-level = 3
debug-assertions = false
overflow-checks = false
incremental = false
codegen-units = 1
lto = "fat"
panic = "abort"
